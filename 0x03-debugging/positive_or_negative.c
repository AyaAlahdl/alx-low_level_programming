#include "main.h"

/**
 * positive_or_negative.c - checks for n and p
 *
 * 0i: the number is change
 *
 * Return: 0
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);
}
